# Unity generated
/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/
/[Bb]uild/
/[Bb]uilds/
/[Ll]ogs/
/[Uu]ser[Ss]ettings/
/[Mm]emoryCaptures/
/[Rr]ecordings/

# Asset meta data should only be ignored when the corresponding asset is also ignored
!/[Aa]ssets/**/*.meta

# Visual Studio / Rider
.vs/
.idea/
.vscode/
*.csproj
*.unityproj
*.sln
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# Unity crash / misc
sysinfo.txt
*.apk
*.aab
*.unitypackage
*.app
crashlytics-build.properties

# OS
.DS_Store
Thumbs.db

# Luban 工具本体（第三方二进制，约 30 MB）。scripts/gen-tables.ps1 发现缺失会自动下载解压，
# 不必进库；生成出来的代码与 .bytes 照常进 git。
/Tools/Luban/

# Claude Code 本机覆盖配置
.claude/settings.local.json

# Claude Code 运行缓存（钩子计数、已读记录、压缩快照）
.claude/.cache/

# Python 脚本（钩子 / lint / 扫描）运行产生的字节码缓存
__pycache__/

# Addressables 每平台的增量更新基线（出包时生成）。只有做 content update 热更才用得上，
# 本工程暂不做；而且每次出包都变，是没法 diff 的二进制。将来要上热更，删掉这几行把它收回跟踪当基线。
# 目录和目录的 .meta 必须一起忽略——只忽略 .bin 会剩下一个指向空目录的 .meta，Unity 会报「meta 没有对应资产」再删掉它。
Assets/AddressableAssetsData/Android/
Assets/AddressableAssetsData/Android.meta
Assets/AddressableAssetsData/Windows/
Assets/AddressableAssetsData/Windows.meta
